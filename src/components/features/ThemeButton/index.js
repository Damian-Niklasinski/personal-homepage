import { ThemeButtonContainer, StyledButtonText, StyledThemeButton } from "./styled";

const ChangeThemeButton = () => (
    <ThemeButtonContainer>
        <StyledButtonText>DARK MODE ON</StyledButtonText>
        <StyledThemeButton />
    </ThemeButtonContainer>
);

export default ChangeThemeButton;